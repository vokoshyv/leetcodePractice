/**
 * @param {number[]} nums
 * @return {number}
 */
var longestConsecutive = function(nums) {
  // var work = [];
  // for (var i = 0; i < nums.length; i++){
  //   work[nums[i]] = 'blah';
  // }

  // work = work.join('OOO');
  // work += 'OOO';
  // work = 'OOO' + work;

  // work = work.split('blah');

  // var max = 0;
  // var current = 0;

  // for (var i = 0; i < work.length; i++){
  //   if (work[i] !== 'OOO'){
  //     max = Math.max(max, current);
  //     current = 0;
  //   } else {
  //     current++;
  //     max = Math.max(max, current);
  //   }
  // }
  // return max;

  
};