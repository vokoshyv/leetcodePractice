/**
 * Definition for a point.
 * function Point(x, y) {
 *     this.x = x;
 *     this.x = y;
 * }
 */
/**
 * @param {Point[]} points
 * @return {number}
 */
var maxPoints = function(points) {
    
};