/**
 * @param {number} n
 * @return {boolean}
 */
var isHappy = function(n) {
    let visited = new Set();

    const transform = (current) => {
        if (current === 1) {
            return true;
        }

    }
};
