/**
 * Definition for singly-linked list.
 * function ListNode(val) {
 *     this.val = val;
 *     this.next = null;
 * }
 */
/**
 * @param {ListNode} head
 * @return {ListNode}
 */
var oddEvenList = function(head) {
  var reference = head;
  var onOdd = true;
  var currentNode = head;
  var temp;

  while (currentNode !== null){
    if 

    onOdd = onOdd? false : true;
  }
};